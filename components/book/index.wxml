<view class="container" bind:tap="onTap">
  <image src="{{book.imgUrl}}"></image>
  <view class="description">
    <text class="title">{{book.bookName}}</text>
    <text class="pub">{{book.pubInfo}}</text>
  </view>
</view>
